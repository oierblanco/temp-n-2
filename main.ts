let temperatura = 0
basic.forever(function () {
	
})
basic.forever(function () {
    if (true) {
        temperatura = input.temperature()
        basic.showNumber(temperatura - 0)
    } else if (input.temperature() < 16) {
    	
    } else {
    	
    }
})
